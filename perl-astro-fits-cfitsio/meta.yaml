package:
  name: perl-astro-fits-cfitsio
  version: "1.18"

source:
  url: https://cpan.metacpan.org/authors/id/P/PR/PRATZLAFF/Astro-FITS-CFITSIO-1.18.tar.gz
  #  patches:
   # List any patch files here
   # - fix.patch

build:
  number: 0

requirements:
  build:
     - {{ compiler('c') }}
     - {{ compiler('cxx') }}
  host:
    - conda-forge::perl =5.32.1 # [linux]
    - perl # [osx]
    - perl-extended-deps ==0.5.0
    - perl-alien-cfitsio ==4.4.0.2
  run:
    - perl
    - perl-extended-deps ==0.5.0
    - perl-alien-cfitsio ==4.4.0.2

#test:
#  # Perl 'use' tests
#  imports:
#    - Ska::Convert

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

