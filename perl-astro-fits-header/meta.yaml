package:
  name: perl-astro-fits-header
  version: "3.09"

source:
  url: https://cpan.metacpan.org/authors/id/G/GS/GSB/Astro-FITS-Header-3.09.tar.gz
  #  patches:
   # List any patch files here
   # - fix.patch

build:
  number: 0

requirements:
  build:
     - {{ compiler('c') }}
     - {{ compiler('cxx') }}
  host:
    - conda-forge::perl =5.32.1 # [linux]
    - perl # [osx]
    - perl-extended-deps ==0.5.0
    - perl-alien-cfitsio
    - perl-astro-fits-cfitsio
  run:
    - perl
    - perl-extended-deps ==0.5.0
    - perl-alien-cfitsio
    - perl-astro-fits-cfitsio

#test:
#  # Perl 'use' tests
#  imports:
#    - Ska::Convert

  # You can also put a file called run_test.pl (or run_test.py) in the recipe
  # that will be run at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

